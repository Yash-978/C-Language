#include<stdio.h>
#include<conio.h>
main()
{
	int A,B;
	printf("Enter the value of A :");
	scanf("%d",&A);
	printf("Enter the value of B :");
	scanf("%d",&B);
	
	(A>B) ? printf("A is maximum.") : printf("B is maximum.");
	
}
