//WAP to given string length.
#include <stdio.h>
main()


{
	int i,j=0;
    char str[]="yash";
    int len=sizeof(str)/sizeof(str[0]);
	
    printf("The length of the string is %d\n", len-1);

}
